apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    db: couchdb0
  name: couchdb0
  namespace: hyperledger
spec:
  selector:
    matchLabels:
      db: couchdb0
  replicas: 2
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        db: couchdb0
    spec:
      containers:
        - name: couchdb0
          image: couchdb:latest
          ports:
            - containerPort: 5984
          env:
            - name: COUCHDB_USER
              value: admin
            - name: COUCHDB_PASSWORD
              value: adminpw
      restartPolicy: Always