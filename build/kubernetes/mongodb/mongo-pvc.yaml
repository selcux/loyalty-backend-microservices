apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-volume
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "azurefile-mongo"
  resources:
    requests:
      storage: 5Gi